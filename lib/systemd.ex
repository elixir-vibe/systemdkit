defmodule Systemd do
  @moduledoc """
  Pure Elixir tools for working with systemd.

  The first supported surface is a D-Bus backed manager client. Unit file
  parsing and generation will live alongside it as the package grows.
  """
end
